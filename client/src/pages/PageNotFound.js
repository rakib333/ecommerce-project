import React from 'react';
import Layout from '../components/Layout/Layout';

const PageNotFound = () => {
    return (
        <Layout>
            <h3>This is page not found</h3>
        </Layout>
    );
};

export default PageNotFound;